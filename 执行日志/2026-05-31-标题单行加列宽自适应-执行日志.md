---
description: 「主题与页面规划」承接上一步——把格子卡标题从"折行(多行)"改为"单行不折行 + 列宽自适应内容(max-content)撑开完整显示"。grid 数据列 minmax(150px,1fr)→minmax(max-content,1fr)、行头列 120px→minmax(120px,max-content)；爸爸/孙子/行头标题 whitespace-nowrap；去掉 flex 链路上的 w-full/min-w-0/truncate 约束让内容撑开。仅改 LoomGrid.tsx。tsc 0、真浏览器验过、零 console error。本地留痕不 commit。
created: 2026-05-31 22:40
updated: 2026-05-31 22:40
---

# 执行日志 · 标题单行 + 列宽自适应

## Sean 指示
"如果表格行可以自适应，那就不用跨行了。"→ 标题别折行，靠列宽自适应内容把标题单行撑开。

## 改动（仅 LoomGrid.tsx）
- `gridTemplateColumns`(L580)：行头列 `120px`→`minmax(120px,max-content)`；数据列 `minmax(150px,1fr)`→`minmax(max-content,1fr)`（列最小宽=内容最宽，绝不截断；有余量仍按 1fr 拉伸）；末列 48px 不变。
- 标题 `whitespace-nowrap`：爸爸(L724)、孙子(L733)、行头英文(L686)/中文(L688)。
- 去夹断约束：爸爸卡容器 `w-full min-w-0`→`w-max`(L712)、爸爸卡头(L714)/孙子行(L729) 去 `w-full min-w-0`，让 nowrap 自然宽度撑开列。

## 验证
- tsc 0；lint 0；console 0 error。
- 真浏览器：长标题(如 "Tasbih for Dhikr · 念珠用于念诵")单行完整不截断；computed gridTemplateColumns 各列宽不同(127/188/174/179/174/181px)内容多者更宽；折叠/下钻/缩放/行头左对齐+pillar 信息回归正常。截图 `验证截图/loom-nowrap-{overview,zoomed-titles,zoomed-out-full}-2026-05-31.png`。

## 已知点
- 极长标题(50+字符)会使该列很宽、需缩放/平移看全（预期）；如需可后续给 shortTitle() 加最大长度安全阀。

## 状态
- 未 commit。当前未提交源码：AssignMenu.tsx + LoomGrid.tsx，叠 6 步于 5ea04aa（深层指派、行头pillar、格子树、可折叠卡、4项调整、本次单行+列宽自适应）。回滚 git checkout LoomGrid.tsx。
