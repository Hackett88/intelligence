---
description: M6 辅助词(实体)前端可编辑交互：增删 chip + reducer SET_AUX_KEYWORDS + 防抖自动落库
created: 2026-06-01 07:18
updated: 2026-06-01 07:25
---

# M6 辅助词可改删 -- 前端执行日志

## 改动清单

| 文件 | 改动类型 | 说明 |
|------|----------|------|
| WorkbenchClient.tsx | Action union + reducer case + props | 加 `SET_AUX_KEYWORDS` action；reducer 仿 SET_PAGE_URL 做 snapshot/undo/去空白去重；传 `onAuxChange` 回调给 InspectorPanel |
| InspectorPanel.tsx | 新组件 + props 透传 + UI 替换 | 新增 `AuxKeywordEditor` 组件（chip + x 删除 + 添加输入）；`onAuxChange` 加入 InspectorPanelProps + PageInspector inline type + 解构；辅助词区从只读 chip 换为可编辑版本 |

## 验收

- tsc --noEmit: 0 errors
- 浏览器自测 (http://localhost:3000/app/strategy):
  - 选中 dk-pillar 页（Dhikr: The Complete Guide...），抽屉显示 5 个辅助词 chip
  - 删除 "adhkar" -- chip 消失，剩 4 个
  - 添加 "test-aux-word" -- 新 chip 出现，共 5 个
  - 防抖 1s 后查库：`["remembrance of Allah","subhanallah","alhamdulillah","allahu akbar","test-aux-word"]` -- 正确
  - 清理：删 test-aux-word，加回 adhkar，最终库恢复原值
- console: 0 new errors
- 截图: `.playwright-mcp/aux-keywords-before-edit.png`, `.playwright-mcp/aux-keywords-after-edit.png`

## 风险/遗留

- reconcile 逻辑中 `auxEmpty` 时回填 seed 的辅助词：当前用户无法"故意清空所有辅助词"（清空后 reconcile 会补回 seed 值）。已有 TODO 注释标记，待加"已编辑"标记后修复。这是既有设计，本次不动。
