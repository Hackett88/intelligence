---
description: 选题工作台「主题与页面规划」关键词进料从静态快照（176 词冻结）改为实时读数据库 keywords 表（现 441 词），与「关键词库」页面同源同步、永不过期。
created: 2026-06-01 17:30
updated: 2026-06-01 18:10
---

# 进料实时读库 · 执行日志

## 一句话目标

「主题与页面规划」的关键词进料过去读 `_all-keywords.ts`（5/29 一次性烤进代码的 176 词静态快照），与读库的「关键词库」页面（现 441 词）不同源 → 库涨到 441 进料仍停在 176。本次把进料改成实时读 DB `keywords` 表（复用现有 60s 缓存层 `getKeywordsCached`），库一变进料就变。

## 改动文件清单

| 文件 | 改动类型 | 一句话理由 |
| --- | --- | --- |
| `src/app/app/strategy/_components/_keyword-source.ts` | 新增 | server 端 `getKeywordSource(): Promise<RawKeyword[]>`，内部 `getKeywordsCached()` → map 成 RawKeyword[]（DB 行→RawKeyword 字段映射 + 兜底）。id = `k${row.id}`。 |
| `src/app/app/strategy/_components/_workbench.ts` | 改签名 | `getWorkbenchSeed(allKeywords: RawKeyword[])`：删 `import { ALL_KEYWORDS }`，改用传入数组；删模块级 `_seed` 缓存（数据会变，缓存交给 getKeywordsCached）。 |
| `src/app/app/strategy/_components/_plan-store.ts` | 改数据源 | 删 `import { ALL_KEYWORDS }` 改 import `getKeywordSource`；`KW_BY_ID`/`ID_BY_NK` 不再顶层静态构建，改在 `loadPlan`/`savePlan` 内部 `await getKeywordSource()` 后就地构建。头注同步更新。 |
| `src/app/app/strategy/page.tsx` | 接线 | `getKeywordSource()` 并入现有 `Promise.all` 并发；`getWorkbenchSeed(allKeywords)`；新增 `allKeywords` prop 传 `<WorkbenchWrapper>`。 |
| `src/app/app/strategy/_components/WorkbenchWrapper.tsx` | 透传 prop | 新增 `allKeywords: RawKeyword[]` prop，透传给 WorkbenchClientDynamic。 |
| `src/app/app/strategy/_components/WorkbenchClient.tsx` | 数据流接线 | 删 `import { ALL_KEYWORDS }`；props 加 `allKeywords`；4 处 `ALL_KEYWORDS`→`allKeywords`（KW_BY_ID / poolKeywords / parkedKeywords useMemo + 传 InspectorPanel），对应 useMemo 依赖数组补 `allKeywords`。**未动任何 UI/样式/交互**。 |
| `scripts/regression-cannibalization.ts` | 最小适配 | `getWorkbenchSeed()`→`getWorkbenchSeed(ALL_KEYWORDS)`（脚本已 import ALL_KEYWORDS，传入即可，语义不变）。 |
| `scripts/_test-m6-plan-roundtrip.ts` | 最小适配 | reconcile 段 `getWorkbenchSeed()`→`getWorkbenchSeed(ALL_KEYWORDS)`；选词从 `getKeywordSource()` 取（savePlan 已改实时库 id 体系 `k<dbid>`，须同源取词才能往返）。 |

> 边界遵守：未删 `_all-keywords.ts`（regression 脚本仍 import）；未碰 `src/db/schema.ts` / 不动 DB 结构 / 仅本地库；未 commit。

## 字段映射（DB keywords 行 → RawKeyword）

```
id: `k${row.id}`            keyword: row.keyword
market: row.market 原值      sv: row.searchVolume      kd: row.keywordDifficulty
intent: row.intent          behaviorIntent: row.behaviorIntent
pagePlanningIntent: row.pagePlanningIntent ?? "品类聚合页"（兜底）
layer: row.layerLevel ?? "四级兜底"（兜底）
questionType: row.questionType
```
排序沿用 getKeywordsCached 的 createdAt 升序。

## 设计取舍

- `_keyword-source.ts` 原计划 `import "server-only"`，但该包未安装、且本仓库其它 server 模块（`_plan-store` / `keywords-cache`）均不用它（靠注释+约定）。tsx 跑临时脚本也解析不到。故改为注释标注 server-only，与既有惯例一致，不引入新依赖。
- DB 实测 pagePlanningIntent / layerLevel 空值占比 0%，兜底形同虚设但仍保留作类型防御（保证非空联合类型不被破坏）。

## 运行命令

```bash
npx tsc --noEmit                              # 验收1：typecheck
npx tsx scripts/_tmp-null-ratio.ts            # 验收5：空值占比（临时，已删）
npx tsx scripts/_tmp-keyword-source-selftest.ts  # 验收2+3：映射+往返（临时，已删）
npx tsx scripts/_test-m6-plan-roundtrip.ts    # 回归：savePlan/loadPlan 往返
npx tsx scripts/regression-cannibalization.ts # 回归：蚕食检测
```

## 五项验收证据

### 1. typecheck（npx tsc --noEmit）

```
tsc exit code: 0
```
零类型错误。ESLint 对 WorkbenchClient.tsx 报的 3 error + 1 warning（refs during render / access before declared / useEffect missing 'state'）经 `git stash` 对比，**改动前同文件已存在、内容逐字一致**，与本次无关，按外科手术原则不顺手清。本次改动未引入任何新 lint 报错。

### 2. getKeywordSource 映射（临时脚本）

```
getKeywordSource().length = 441; DB keywords count = 441   → 一致
kwId 全唯一 / 全形如 k<dbid>                                 → ok
抽查 DB row id=200 "black zikr ring" market=us：keyword/market/sv/kd/intent/
  behaviorIntent/pagePlanningIntent/layer/questionType 九字段映射全 ok
抽查最高 SV row id=72147 "when is ramadan 2025" sv=60500：sv=60500, layer=二级独立 一致
```

### 3. admin@weslamic.com loadPlan 恢复（临时脚本）

```
DB strategy_bindings(owner=admin@weslamic.com): 总 58（bound=58, parked=0）
自然键反查：对得回 58 / 58，跳过 0
loadPlan 恢复：bindings=58, parked=0（pages=20）
恢复总数 58 vs DB 绑定行 58                → 完全吻合
所有恢复出的 kwId 都存在于实时进料中        → ok
```
**跳过 0 条**——库换 kwId 体系（`k<dbid>`）后，58 条历史绑定（按自然键 keyword+market 存）全部正确反查回新 kwId，无丢失。

### 4. 空值占比（临时脚本）

```
total keywords = 441
page_planning_intent NULL/空 = 0 (0.0%)
layer_level          NULL/空 = 0 (0.0%)
behavior_intent      NULL/空 = 0 (0.0%)
market               NULL/空 = 6 (1.4%)
distinct page_planning_intent: 工具生态页/品牌主页/门店页/场景使用页/产品详情页/品类聚合页/知识深度页（7种，全在 PagePlanningIntent 联合内）
distinct layer_level: 二级独立/四级兜底/一级核心/三级变体（4种，全在 LayerLevel 联合内）
distinct behavior_intent: 官网导航/了解型/线下到访/混合型/行动型/对比型（6种，全在 BehaviorIntent 联合内）
```
pagePlanningIntent / layerLevel 空值占比均 **0.0%**，远低于 30% 阈值，无需停下回报。market 6 条空（1.4%）保留原值即可，与 binding 自然键一致。

### 5. 回归测试

- **`_test-m6-plan-roundtrip.ts`**：`✅ ALL PASS — savePlan/loadPlan 往返无损，aux_keywords 落库正确`。现用实时库 id（k177/k143/k161），kwId↔自然键往返无损。
- **`regression-cannibalization.ts`**：独立跑通到 `=== 回归结束 ===`，无报错/崩溃。脚本内写死页面 id 的样本断言（iTasbih pillar×online、智能指环 collection×blog 等）显示「✗ 不符」，但 `git stash` 对比确认**改动前同样全部「✗ 不符」、输出逐字一致**——是更早蓝图重构遗留的脏样本断言，与本次进料改造无关。本次未破坏该脚本。

## 风险 / 回滚

- **风险（低）**：进料随库实时变化，新增/删除关键词会改变 pool/bindings 集合——这正是需求目标。历史绑定按自然键存，kwId 体系换不影响已落库数据（已用 58 条 admin 绑定验证 0 丢失）。
- **缓存延迟**：`getKeywordsCached` 60s TTL，库写入后最长 60s 才反映到进料（写库处已调 `invalidateKeywordsCache` 失效，通常即时）。可接受。
- **回滚**：本次纯代码改动、未 commit、未碰 DB。回滚只需 `git checkout` 改动文件即可恢复静态快照进料。

## 留痕

- 临时脚本 `scripts/_tmp-null-ratio.ts`、`scripts/_tmp-keyword-source-selftest.ts` 已跑完即删。
- 证据输出见本文「五项验收证据」段。
- 浏览器端到端实测（进料数 / 命中变绿 / 老绑定恢复）见后续主控实测记录。
