---
description: 「主题与页面规划」格子内"爸爸→孙子缩进树"三处调整——① footer 去掉"N父·M子"计数(只留 SV+渐变条)；② 每个爸爸的孙子做成可折叠(点 ▸/▾ 收起/展开，stopPropagation 不误触格子下钻)；③ 每个爸爸连同其孙子外包一层边框(成带框小卡)。仅改 LoomGrid.tsx overview 渲染 + 1 个折叠 useState。tsc 0、真浏览器全 PASS、零 console error。本地留痕不 commit。
created: 2026-05-31 21:30
updated: 2026-05-31 21:30
---

# 执行日志 · 格子爸爸卡：可折叠 + 包边框 + 去父子计数

## Sean 指示
"把一父一子这种描述去掉；确保缩进是可折叠的形态；每个 sub-pillar 包含其子 Cluster Pages 额外包一层边框。"

## 改动（仅 LoomGrid.tsx，overview 格子渲染 ~L694-740 + state）
1. import 补 `ChevronDown`(L14)。
2. 新增折叠状态：`collapsedSubs: Set<string>` + `toggleSub(id)`（默认展开，id 进 Set=折叠）。
3. 每个爸爸 = 一张带框可折叠小卡：
   - 外框 `border border-manor-line/30 rounded px-1 py-0.5 mb-1`（"额外包一层边框"）。
   - 卡头：有孙子才显示折叠箭头(`ChevronRight` 折叠/`ChevronDown` 展开，size10)，`onClick` **stopPropagation + toggleSub**（防误触格子下钻）；无孙子留 10px 占位、不显箭头。后接 ◈RoleMark + StatusDot + 标题。
   - 卡身：仅 `hasChildren && !isCollapsed` 时渲染孙子行(pl-3 + `└` + ○ + StatusDot + 标题，封顶3条+"+N more")。
4. footer：删 "N父·M子"，只留 `formatSv(totalSv)` + SV 渐变条。
- 不动 cells 取数/数据/reducer/抽屉/行头；折叠是纯前端视图态、不持久化。

## 验证
- tsc 0；lint 0。
- 真浏览器（程序化 + 截图）：footer 无父/子字样只剩 SV(1650/380/930)；送礼格 2 张带框卡；点箭头 aria-label Collapse↔Expand、孙子 DOM 隐现、点后仍在 overview(stopPropagation 生效)；点格子空白仍进下钻；gap 不变；console 0 error。截图 `验证截图/loom-collapsible-cards-*` / `loom-cards-closeup-{collapsed,expanded}-2026-05-31.png`。
- 主 Claude 复核 L694-740：边框、stopPropagation、无孙子占位、!isCollapsed 守卫、footer 去计数，均正确。

## 状态
- 未 commit。当前未提交源码：AssignMenu.tsx + LoomGrid.tsx，叠在 5ea04aa 上的四步（深层指派、行头pillar、格子树、本次可折叠卡）。回滚 `git checkout -- LoomGrid.tsx`。
