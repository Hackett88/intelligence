---
description: M6 建表执行日志 —— 复活 strategy_pages / strategy_bindings + aux_keywords 列(0016) + scenario_id/aux_edited 列(0017)，本地库 127.0.0.1:5433，全绿。
created: 2026-06-01 18:30
updated: 2026-06-01 19:10
---

# M6 · 建表执行日志

## 1. 派单理解

复活被 0015 清掉的两张选题工作台表（strategy_pages / strategy_bindings），在 strategy_pages 新增 aux_keywords JSONB 列（辅助词数组）。只操作本地库，Sean 已授权。

## 2. 执行环境

- 数据库：PostgreSQL，127.0.0.1:5433（本地库，via SSH tunnel）
- DATABASE_URL：来自 .env.local，host 段确认为 @127.0.0.1:5433，非生产
- DB client：postgres-js，ssl:false
- 工具：pnpm exec tsx

## 3. 改动清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `drizzle/0016_strategy_plan_revive.sql` | 新建 | 复活两表 DDL + aux_keywords 列 + 回滚注释 |
| `scripts/_apply-0016-strategy-plan-revive.ts` | 新建 | 应用脚本：读 .env.local、sql.unsafe 执行、8 项 information_schema 验证、sql.end() |

## 4. 关键 SQL

**Up（核心片段）：**
```sql
CREATE TABLE IF NOT EXISTS strategy_pages (
  ...
  aux_keywords JSONB NOT NULL DEFAULT '[]'::jsonb,  -- M6 新增
  ...
  CONSTRAINT uq_strategy_pages_owner_page_id UNIQUE (owner, page_id)
);
CREATE INDEX IF NOT EXISTS idx_strategy_pages_owner ON strategy_pages (owner);

CREATE TABLE IF NOT EXISTS strategy_bindings (
  ...
  CONSTRAINT uq_strategy_bindings_owner_kw_market UNIQUE (owner, keyword, market)
);
CREATE INDEX IF NOT EXISTS idx_strategy_bindings_owner         ON strategy_bindings (owner);
CREATE INDEX IF NOT EXISTS idx_strategy_bindings_owner_page_id ON strategy_bindings (owner, page_id);
```

**Down（回滚）：**
```sql
DROP TABLE IF EXISTS strategy_bindings;
DROP TABLE IF EXISTS strategy_pages;
```

## 5. 执行命令

```bash
pnpm exec tsx scripts/_apply-0016-strategy-plan-revive.ts
```

## 6. 验证输出（实际输出）

```
Public tables BEFORE migration: 99
Applying: .../drizzle/0016_strategy_plan_revive.sql
OK: DDL executed.

--- Verification: tables ---
  FOUND: strategy_bindings
  FOUND: strategy_pages

--- Verification: aux_keywords column ---
  column_name   : aux_keywords
  data_type     : jsonb
  column_default: '[]'::jsonb

--- strategy_pages columns ---
  id                   bigint
  owner                text
  page_id              text
  role                 text
  pillar_id            text
  title                text
  primary_keyword      text
  page_type            text
  status               text
  url                  text
  market               text
  markets              jsonb
  theme_id             text
  theme_name           text
  theme_latin          text
  territory            text
  note                 text
  sort_order           integer
  aux_keywords         jsonb
  created_at           timestamp with time zone
  updated_at           timestamp with time zone

--- Verification: UNIQUE constraints ---
  strategy_bindings: uq_strategy_bindings_owner_kw_market
  strategy_pages: uq_strategy_pages_owner_page_id

--- Verification: indexes ---
  strategy_bindings: idx_strategy_bindings_owner
  strategy_bindings: idx_strategy_bindings_owner_page_id
  strategy_bindings: uq_strategy_bindings_owner_kw_market
  strategy_pages: idx_strategy_pages_owner
  strategy_pages: uq_strategy_pages_owner_page_id

Public tables AFTER  migration: 101
Table count delta: 2 (expected 2)

--- Final Verdict ---
  [PASS] Both tables exist
  [PASS] aux_keywords col is jsonb
  [PASS] UNIQUE on strategy_pages
  [PASS] UNIQUE on strategy_bindings
  [PASS] idx_strategy_pages_owner
  [PASS] idx_strategy_bindings_owner
  [PASS] idx_strategy_bindings_owner_page_id
  [PASS] Table count +2

Verdict: PASS — migration complete, all checks green.
```

## 7. 建表前后表数对比

| 时间点 | public 表数 |
|--------|------------|
| 建表前 | 99 |
| 建表后 | 101 |
| Delta  | +2（strategy_pages + strategy_bindings）|

## 8. 回滚预案

```bash
# 回滚命令（本地库执行，两表无业务数据，秒级完成）
psql $DATABASE_URL -c "DROP TABLE IF EXISTS strategy_bindings; DROP TABLE IF EXISTS strategy_pages;"
```

或重跑 0015 的 down SQL（内容相同）。预计恢复时间：< 5 秒。

## 9. 未决问题

1. schema.ts（Drizzle ORM model）尚未更新，等 app-backend 接手时补写 TypeScript 表定义。
2. 生产库是否同步建表，另行授权决策，本次未触碰。
3. aux_keywords 字段的 JSON 结构（string[] 还是 `{text,id}[]`）由 app-frontend 和 app-backend 协商，当前 JSONB 兜得住两种结构。

---

## 0017 补丁：strategy_pages 加 scenario_id / aux_edited（2026-06-01 19:10）

**Migration 文件：** `drizzle/0017_strategy_pages_scenario_auxedited.sql`
**应用脚本：** `scripts/_apply-0017-strategy-pages-scenario-auxedited.ts`
**应用命令：** `pnpm exec tsx scripts/_apply-0017-strategy-pages-scenario-auxedited.ts`

**变更内容：**
```sql
ALTER TABLE strategy_pages ADD COLUMN IF NOT EXISTS scenario_id TEXT;
ALTER TABLE strategy_pages ADD COLUMN IF NOT EXISTS aux_edited BOOLEAN NOT NULL DEFAULT false;
```

**验证输出（6/6 全绿）：**
```
strategy_pages rows BEFORE: 20

  aux_edited  type=boolean  nullable=NO   default=false
  scenario_id type=text     nullable=YES  default=NULL

strategy_pages rows AFTER:  20  (delta=0)
Rows with aux_edited=true (expect 0): 0
Rows with scenario_id NOT NULL (expect 0): 0
strategy_bindings still exists: true

[PASS] scenario_id: text, nullable
[PASS] aux_edited: boolean, not null, default false
[PASS] Row count unchanged
[PASS] Existing rows aux_edited=false
[PASS] Existing rows scenario_id=NULL
[PASS] strategy_bindings untouched

Verdict: PASS — columns added, all checks green.
```

**回滚 SQL：**
```sql
ALTER TABLE strategy_pages DROP COLUMN IF EXISTS aux_edited;
ALTER TABLE strategy_pages DROP COLUMN IF EXISTS scenario_id;
```
