---
description: 「主题与页面规划」选题工作台清成白板重搭——分 6 批：①备份旧规划数据 ②拆蚕食 UI（抽屉/标签/Worklist 三处）③拆 DB 持久化层（schema 表定义/_plan-store/_plan-actions/page 接线全删，回退到 localStorage+种子）④种子清成白板骨架（4 光杆品类 pillar + 列重整为知识/场景/工具/品牌 4 大列 + Zikr Ring×品牌 放 WESLAMIC 品牌起点 + 升 localStorage key 顺带清本地旧草稿，⑥并入此批）⑤删 strategy_pages/strategy_bindings 两表（本地库，备份在先，其余 88 表无损）。tsc 0、真浏览器验过、本地留痕不 commit。
created: 2026-05-31 08:00
updated: 2026-05-31 08:00
---

# 执行日志 · 选题工作台清白板重搭

## Sean 指示（汇总）
"先把抽屉中的页面关系部分去掉，把标签页中的蚕食信息也去掉，还要把绑定的目标 URL 都解绑掉；当前只有知识/场景/工具 3 类，从关键词库把其它类型也补进来（要品牌页）。这些页面和关键词都是做这个页面时 agent 自己配的、本来就不是我们要的，直接清除即可，包括对应的数据库表都可以删掉。" 后续细化：① 蚕食信息（抽屉页面关系 + 标签页）都去掉；③ 解绑用 A（直接清除）；表**真的删**（不需要持久化）；**行先留着**（重建需要骨架）；**品牌带点内容**。并确认白板 mockup：品牌起点放在 Zikr Ring × 品牌。

## 战略背景（贯穿全程的判断锚）
WESLAMIC **只卖一个产品 = zikr ring**，其余全是为它做内容引流。这张表的终极形态：左上=流量入口、右下=流量终点(zikr ring)，对角线漏斗。这批清白板就是为"按这个漏斗重新搭"腾干净地基。

## 分批执行

### ① 备份（前置保护）
- 本地无 psql/pg_dump/Docker，用 Node + `postgres` 包导出。
- `备份/strategy_pages_backup_2026-05-31.json`（31 行，23KB）、`备份/strategy_bindings_backup_2026-05-31.json`（116 行，32KB）。**本地留存、不进 git**。

### ② 拆蚕食 UI（3 文件）
- `WorkbenchDock.tsx`：删蚕食面板/入口。
- `InspectorPanel.tsx`：删"页面关系"段。
- `Worklist.tsx`：删"蚕食待解"段。
- 引擎 `detectCannibalization` 等保留休眠（_workbench.ts），仅去 UI。

### ③ 拆 DB 持久化层（回退 localStorage + 种子）
- 删 `_plan-store.ts`、`_plan-actions.ts`。
- `src/db/schema.ts`：移除 strategyPages/strategyBindings 表定义 + 类型。
- `page.tsx` / `WorkbenchWrapper.tsx` / `WorkbenchClient.tsx`：移除 initialPlan 接线、saveStrategyPlanAction 防抖保存、conflicts(蚕食) useMemo+props。
- 关键顺序：**先拆引用代码、再删表**（否则 app 查不存在的表会报错）。tsc 0、build green、无残留引用、DB 未动。

### ④ 种子清成白板 + 列重整 + 品牌起点（⑥并入）
- `_data.ts`：THEMES 重写为 **4 个光杆品类 pillar**（islamic-jewelry / name-necklace / tasbih / zikr-ring，id 对齐行脊 CATEGORY_SPINE）——每个 role=pillar、status=gap、真实 `/collections/*` URL、**keywords:[]**、clusters:[]。**唯一内容**：zikr-ring.clusters 挂一张品牌起点子支柱 `zr-sp-brand`（role=sub-pillar、**scenarioId="brand"**、parentId="zr-pillar"、status=live、url=/collections/all、3 个品牌词 weslamic/weslamic.com/weslamic official）。保留全部类型/kw()/rollup/EXCLUDED 机器；UNASSIGNED 清空。
- `LoomGrid.tsx`：`WEFT_GROUPS` 重整为 **4 个类型大列**（knowledge/scenario/tool/brand，各带一同名小列），品牌列最右=最靠近转化。`TERRITORY_BY_THEME` 同步裁掉已删主题。
- **升 localStorage key 顺带清本地旧草稿**（=⑥）：`STRUCT_KEY` v2→v3（LoomGrid）、`STORAGE_KEY` wb-session-v2→v3（WorkbenchClient）。旧本地草稿被忽略，自动加载新白板种子+4 列默认。

### ⑤ 删两表（本地库）
- `drizzle/0015_drop_strategy_plan.sql`：`DROP TABLE IF EXISTS strategy_bindings; DROP TABLE IF EXISTS strategy_pages;`（无 FK，bindings→pages，索引随表删）。
- `scripts/_apply-0015-drop-strategy-plan.ts`：仿 0008 模板，sql.unsafe + information_schema 验证两表消失。
- 执行 `pnpm exec tsx scripts/_apply-0015-drop-strategy-plan.ts` → PASS。删前行数 31/116 对得上备份；删后两表消失、**其余 88 表全在**（keywords/gsc_*/ga4(insights)/query_history/user/auth 逐一确认）。

## 验证
- tsc 0；lint 0；next build green（③后）。
- 真浏览器（④后，因 key 升级自动白板）：列=知识/场景/工具/品牌（4 列）；行=饰品/项链/念珠/Zikr Ring（4 行，行头 ◆pillar+gap）；除 **Zikr Ring×品牌 = ◈WESLAMIC 品牌主页** 外格子全空(gap)；词池 ~187 词回流（库内 3 个品牌词绑在品牌页、4 噪声排除）；Zikr Ring 下钻见光杆 pillar+品牌子支柱；console 0 error；加列/加行/缩放/平移/拖拽/指派菜单不回归。截图 `验证截图/loom-grid-whiteboard-*-2026-05-31.png`、`loom-grid-zikr-ring-drilldown-2026-05-31.png`。
- 主 Claude 自核：_data.ts THEMES（4 光杆+品牌子支柱、kw 参数顺序对、scenarioId/parentId 对）、WEFT_GROUPS（4 大列）、两个 key v3、0014 表名/无 FK、删后表清单——均属实。

### ④补 · 小列找回（收过头的更正）
- Sean 反馈：原小列 Gifts/Slow Living/Qibla/iTasbih 是需要的，不该砍。从 git 3b5c39e 捞回准确 id/标签，按漏斗序补回 `WEFT_GROUPS`：知识=[knowledge-dhikr 念诵]；场景=[slow-living 慢生活, muslim-gifts 送礼]；工具=[qibla-finder 朝向, itasbih-tools 数字念珠]；品牌=[brand 品牌]（最右保留）。
- 因 v3 单列默认可能已被缓存，`STRUCT_KEY` v3→**v4** 强制新默认生效。
- 验证：tsc 0；浏览器 reload 后 localStorage v4 落 {知识:[念诵],场景:[慢生活,送礼],工具:[朝向,数字念珠],品牌:[品牌]}；4 大列 6 小列渲染正确、白板与品牌起点不变。截图 `验证截图/loom-grid-restore-small-cols-2026-05-31.png`。主 Claude 自己改自己验（小改，未外派）。

### ④补2 · 列宽自适应核查 + 表头 nowrap
- Sean 反馈"列没能自适应宽度、内容会截断"。现场量证（chrome-devtools-local）：`gridTemplateColumns` 数据列为 `minmax(max-content, 1fr)`，**自适应在工作**——品牌列因 WESLAMIC 卡片(w-max+nowrap 标题)撑到 152px，空列才 94px；品牌格标题 "WESLAMIC 品牌主页" nowrap、scrollWidth==clientWidth、**未截断**；当前空板无任何元素 trunc。
- 唯一隐患：小列表头 en/zh 未加 nowrap（L651-652）。已补 `whitespace-nowrap`，使"列宽 ≥ 表头完整宽度"成铁律，表头永不折行。tsc 0、reload 复测通过。截图 `验证截图/loom-grid-cols-autowidth-2026-05-31.png`。
- 结论：列宽=各列内容(表头+格内卡片，均 nowrap)的 max-content，绝不截断；空板下 6 列均分中栏宽度故偏窄(~94px)、有内容即按列撑开。是否要更宽列(代价=横向滚动/平移)待 Sean 定。

## 状态 / 回滚
- **未 commit**（含本日志，按约定本地留痕不入库）。当前未提交源码累积很多：LoomGrid/AssignMenu 多步 + 拆蚕食 4 文件 + 拆持久化 6 文件 + 本批 _data/WEFT/key/_workbench，叠在最近 commit 之上。是否提交待 Sean 拍板。
- 回滚：源码 `git checkout -- <文件>`；DB 重跑 0014 建表 + 备份 JSON 回灌（约 5 分钟）。
- **遗留待 Sean 手动验**：滚轮缩放"以光标为锚不漂移"（合成 WheelEvent 测不了，前一批已代码层修正+复核）。
- **红线提醒**：`备份/` 目录是唯一数据恢复来源，勿误删。
