---
description: UI 文字可读性优化循环(cron 3d10a125,每轮结束≈60s 后自动下一轮)——第1轮:全局 ink 色阶提亮(inkFaint 2.6:1→4.4:1 / inkGhost 1.6:1→3.3:1)+ 弹窗点位修复
created: 2026-07-04 04:10
updated: 2026-07-04 04:10
---

# 可读性优化循环(未推送,攒批中)

循环机制:cron job 3d10a125(*/1,仅空闲时触发 = 每轮结束约 60s 后自动开下一轮,7 天自动过期)。
每轮:agent 扫描 → 亲自浏览器核验 → 修复 → tsc+复验 → 本地 commit(禁推)。

## 第 1 轮(2026-07-04 04:10)

**根因(亲测)**:主题 ink 色阶后两档过暗——bg2(#08130D) 上 inkFaint #5D5A4F 对比度仅 ≈2.6:1、
inkGhost #3D3A33 仅 ≈1.6:1(可读线 4.5:1,底线 3:1),而这两档被大量用于信息文字
(弹窗次要行、到期标签、提示语),正是 Sean 截图里看不清的全部内容。

**修复**:
1. 全局提亮(globals.css):inkFaint → #837D6D(≈4.4:1)、inkGhost → #6E695C(≈3.3:1);
   层级 ink(14:1)>inkDim(5.9:1)>inkFaint>inkGhost 保持不变,全 APP 普涨受益;
2. 点位:刷新收录/请求索引弹窗次要行 9.5px→10.5px;到期标签与「未请求过」由 inkGhost 升 inkDim
   (可操作信息不该用最弱档)。

**验证**:改前/改后截图对比(test-evidence/readability-before-inspect-dialog.png + 对话内截图),
同一弹窗肉眼全部可读;tsc 全绿。

**待办(下一轮)**:两个审计 agent(indexing 模块清单 / 全局 token 使用面)报告未回,
第 2 轮开始时消化其清单,继续逐点核验修复(keywords/strategy 页、抽屉、SummaryBar 等)。

## 第 2 轮(2026-07-04 04:15)—— 消化两份 agent 审计清单

两个审计 agent 回报(indexing 模块逐点清单 / 全站 token 使用面):inkFaint 全站 187 处
几乎全是信息文字(提亮=普涨受益,判断与 R1 一致);inkGhost 36 处多为"缺席/禁用"应保持最暗,
仅少数承载信息处需升档。R1 已顺手修掉清单中 4 处(9.5px 行、到期标签、未请求过徽章)。

本轮逐点修复(9 处,均亲验后改):
- DetailDrawer:Google 诊断原话 10px faint→11px dim(诊断价值高);两处空态 ghost→faint;
- PageTable 空表消息 ghost→faint;
- 用量面板:近7天表头 faint→dim、"(洛杉矶日)" ghost→faint;
- keywords 两处空态引导语 ghost→dim(空态下最该被读到的"下一步"提示);
- LoomGrid 集群 kw/pages 计数 9px→10.5px(strategy 侧最痛的"最暗×最小"组合)。

有意放过(记录判断):em-dash"—"占位/分隔符/禁用态保持 inkGhost(语义=缺席,故意安静);
formatCtr 最低档 faint 是刻意分层;拉丁装饰小标是风格。

验证:tsc 全绿;strategy 页浏览器实测布局无破、左栏与网格文字清晰;收录弹窗复验可读。
